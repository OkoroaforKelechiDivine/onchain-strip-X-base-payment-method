name: pay_me_mobile
description: Payme Mobile Application

publish_to: 'none'

version: 1.0.8+10

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.1
  intl: ^0.19.0
  grouped_list:
  dio: ^5.1.1
  provider:
  connectivity:
  jwt_decode:
  flutter_secure_storage: ^8.1.0
  get_it: ^7.6.0
  flutter_screenutil: ^5.7.0
  page_transition: ^2.1.0
  overlay_support: ^2.1.0
  hive: ^2.2.3
  logger: ^1.3.0
  hive_flutter: ^1.1.0
  stacked: ^3.4.1+1
  pinput: ^3.0.1
  flutter_svg: ^1.1.5
  jwt_decoder: ^2.0.1
  loading_animation_widget: ^1.2.0+4
  url_launcher: ^6.2.2
  share_plus: ^7.2.1
  restart_app: ^1.2.1
  pdf: ^3.6.0
  permission_handler: ^10.0.0
  package_info_plus: ^3.1.2
  http: ^0.13.3
  path_provider: ^2.0.8
  flutter_pdfview: ^1.3.2
  focused_menu: ^1.0.5
  pdfx: ^2.6.0
  #Firebase
  firebase_core: ^2.24.2
  firebase_messaging: ^14.7.10
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
          image_path: "assets/png/appIcon.png" 
          android: true
          ios: true
          # optionally, as transparency is not allowed on app store
          remove_alpha_ios: true

flutter:
  uses-material-design: true

  assets:
    - assets/jpg/
    - assets/png/
    - assets/svgs/
